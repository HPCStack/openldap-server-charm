name: openldap-server
summary: OpenLDAP server (slapd) as backend of OpenStack's Keystone
maintainer: Yuriy Shestakov <yshestakov@gmail.com>
description: |
  This is the OpenLDAP (Lightweight Directory Access Protocol) server
  (slapd). The server can be used as OpenStack's Keystone backend.
tags:
  # Replace "misc" with one or more whitelisted tags from this list:
  # https://jujucharms.com/docs/stable/authors-charm-metadata
  - misc
  - openstack
categories:
  - databases
subordinate: false
provides:
  ldapdb:
    interface: ldap
#        name: ldap
#        variables_set:
#            - bind_dn
#            - bind_pw
#            - base_dn
#            - server_addr
